# Versie 1 · 2026-07-19 (updated 2026-07-21)

Status: **In progress; production remains Apps Script immutable v55, immutable v56 is historical, and immutable v57 is staged owner-only pending external quota recovery**. Fresh OAuth for a new account and second-account live fan-out remain unverified. Product source is integrated into `main` through PR #1/#2/#4/#7/#8.

## Why Versie 1

This is the first version of the new sequential product line. Old v27-v47 artifacts are not deleted: they remain technical and research links, but no longer compete with the product number. Versie 1 consolidates all verified functionality from the preceding line and adds the current incident fixes.

## Cumulative history

- [v27 sanitized baseline](../../audit/v27-baseline-2026-07-18.md): safe Apps Script and Telegram bridge baseline.
- [v28 neuroinclusive roadmap](../../product/neuroinclusive-v28-roadmap.md) and [focus view](../../design/focus-view-v28-spec.md): ADHD-friendly navigation and focus.
- [v39 trustworthy summary](../../product/trustworthy-summary-v39.md): explainable mail summaries.
- [v40 backlog rescue](../../product/backlog-rescue-v40.md): controlled backlog processing.
- [v41 functional relief metrics](../../product/functional-relief-metrics-v41.md): content-free progress.
- [v42 calendar/task handoff](../../product/calendar-task-handoff-v42.md) and [session capacity](../../audit/v42-session-capacity-2026-07-18.md): safe sessions and handoff.
- [v43 adaptive density](../../product/adaptive-density-v43.md): adaptive interface density.
- [v44 co-processing presence](../../operations/v44-co-processing-presence.md): private co-working support.
- [v45 gentle milestones](../../product/gentle-milestones-v45.md), [roadmap status](../../audit/neuroinclusive-roadmap-status-v45.md), and [release readiness](../../audit/v37-release-readiness-2026-07-18.md): stable production Apps Script v37.

## What Versie 1 inherits

- private Telegram Mini App mail client;
- account/zone-scoped Gmail sessions;
- read, search, folders, attachments, drafts, and guarded mail actions;
- neuroinclusive focus, backlog rescue, gentle milestones, and content-free metrics;
- Drive/Box attachment-source isolation;
- send-later, workspace roles, session replay protection, and bounded recovery;
- immutable release helpers and rollback evidence.

## New Versie 1 changes

- GT-001: removed duplicate owner Gmail scanning through legacy and OAuth lanes without merging unrelated connection records.
- GT-002: Google returns to a neutral GitHub Pages callback that clears the URL and transfers only one-use `state/code` to Apps Script through a credentialless POST.
- GT-003: desktop/mobile header uses the Google profile photo with fallback.
- GT-004: Google OAuth opens in one click; fallback appears only when navigation is blocked.
- GT-005: stale/inactive account IDs no longer inflate unified count.
- GT-009: corrected the singular accessibility label.
- GT-011: Telegram settings now include native callback buttons for one-click switching and a short GitHub launcher for adding Gmail.
- GT-012: the browser no longer navigates to an Apps Script callback, so Google multi-login cannot rewrite the URL to a broken `/macros/u/N/` route.
- GT-013: owner `/settings` opens the native account menu instead of the fallback.
- GT-014: the production menu again exposes `📬 Mail · Versie 1` as a Web App, but it opens a neutral GitHub Pages bridge rather than a direct Apps Script URL.
- GT-015 through GT-017 separately track duplicate display records, Telegram’s system Open Link warning, and legacy Mini App deep links; no Gmail record was deleted or mixed.
- OAuth state v2 is TTL-bound, one-use, and tied to the exact Telegram user/chat and mail zone; the bot sends the refreshed account menu after success.
- Gmail OAuth prompt is reduced to `select_account`; any new consent is still controlled by Google and completed only by the owner.
- GT-010: token refresh is protected by a per-connection lease and short claim/commit/release locks with no network I/O under lock.
- GT-018/GT-019: the bounded realtime lane runs before frozen backlog; manual `/check` uses the same dedupe ledger.
- GT-023: immutable v56 limits worker cadence to 150 seconds and full Gmail History backfill to one 15-minute slot with no network I/O under ScriptLock.
- GT-024: the same network error on production v56 and exact-rollback v55 is classified as a shared blocker; no candidate-specific regression is claimed without A/B evidence.

## Verified evidence

- Apps Script dashboard: one `checkNewMail_` trigger; no second notifier project was confirmed.
- Versie 1 local contracts: **432/432 passed**; GT-010 separately passed 148/148, release 3/3, and docs 44/44.
- Bridge-only PR #2 merged as [`a7df53c`](https://github.com/Tarasevych/gmail-telegram-controls/commit/a7df53c7b1002b91479d25f825a8de3db1a2ba80); product PR #1 merged normally as [`ee9286e`](https://github.com/Tarasevych/gmail-telegram-controls/commit/ee9286e1148c0147edd3d245c28db2a82a0ea465).
- Immutable v55 passed staging E4; the stable deployment advanced v50→v55. Staging was removed after production E5.
- Final PreflightOnly: stable v55, `headState=candidate_v55`, immutable ready, staging 0, legacy staging 0, journal `cleaned`; pinned MultiAccount hash `8d07e8b9f0f524ed5cedccbb8bfecbb547c93a34eda8ef876e40776d6b470f10`.
- The GitHub Pages relay retains no query, storage, or cookies and posts the callback only to the exact stable Apps Script endpoint.
- The `/macros/u/N/` cause is confirmed by the [official Apps Script troubleshooting guide](https://developers.google.com/apps-script/guides/support/troubleshooting): multi-login is unsupported for Apps Script web apps.
- OAuth redirect URI is saved in the Google OAuth client and read back without mismatch.
- Staging E4: the mailbox opened without a Drive error, the profile photo rendered, three isolated Gmail connections appeared, and one-click switching plus restoration of the original account context passed.
- Production menu `📬 Mail · Versie 1` opens the static GitHub Pages bridge; the production mailbox, AI card, and profile photo were read back.
- Production E5: a controlled owner self-message with Gmail labels `SENT+INBOX` automatically produced one Telegram card. After two `/check` runs, exact-marker search remained at `1 message`.

## Recovery checkpoint after immutable v56

- PR #4 merged normally as [`2392714`](https://github.com/Tarasevych/gmail-telegram-controls/commit/23927148cfa616dbd1504e81768d013b01a9ed37). The full suite passed `440/440`, the additional release tests passed `5/5`, and required GitHub checks succeeded.
- Staging v56 passed signed Telegram Desktop bootstrap, opened the mailbox without a Drive error, and showed the avatar plus three Gmail roots. The automated switch produced no verifiable state change, so that item is not marked as passed.
- Production v56 showed a network error. The exact rollback restored stable v55 and preserved immutable staging v56; the same error reproduced in two v55 launches.
- Apps Script Executions confirmed completed `doPost`, session redemption/renewal, and `mailboxRpc` on v56 and v55. Concurrent worker failures carried the exact `Service invoked too many times for one day: urlfetch` code in the Gmail API path.
- Current release position: stable and HEAD v55, staging v56 preserved, journal `rolled_back`, and the Telegram menu points to production. v56 is not promoted again before two clean v55 launches and the full staging A/B.
- The default GCP project was not migrated; mail, tokens, `initData`, identifiers, and secret properties were not published to GitHub.
- Source request: `REQ-0019`.

## Not yet proven

- The shared `URLFETCH` daily quota blocks a clean v55-to-candidate A/B; recovery time remains unverified (GT-023/GT-024).
- Fresh OAuth account choice/consent/callback for a new Gmail account is unverified; existing sessions do not prove that path.
- Independent realtime fan-out from a second connected account is unverified.
- One first production open remained on the skeleton for more than 15 seconds and loaded the mailbox after refresh (GT-021).
- `clasp logs` is blocked because local config has no GCP project ID; project identity was not guessed (GT-022).
- Versie 1 is not yet tagged as a completed product release; creating Versie 2 requires a separate exact owner instruction.

## Release gates

1. Completed: save and read back the new OAuth redirect URI.
2. Completed: the neutral bridge was published through PR #2; the menu Web App does not use a direct Apps Script URL.
3. Completed for existing sessions: staging E4, profile photo, three account roots, and one-click switching without a Drive error. Fresh OAuth remains a separate gate.
4. Completed for the owner lane: production E5, one card, two `/check` runs, no duplicate.
5. Completed: stable v55, staging 0, legacy staging 0, immutable rollback v50 preserved.
6. This paired UK/EN evidence package updates [ISSUES](../ISSUES.md), [ROADMAP](../ROADMAP.md), and release history.
7. Create the tag and immutable product release branch only after the fresh-OAuth/second-account decision; do not open Versie 2 without the exact owner instruction.
8. Before any new promotion: obtain two fresh production v55 launches without a network error, then run the owner-only staging v56/v57 A/B; if the failure is shared, keep stable v55 and the shared blocker open.

Previous product article: [v45 gentle milestones](../../product/gentle-milestones-v45.md). The next article after release can only be `Versie 2` and must link to this file.

## Current continuation: immutable v57

- PR #7 merged the connection-scoped Gmail metadata identity fix; PR #8 merged the isolated v57 staging launcher. Required GitHub checks passed.
- The complete local Apps Script suite passed `443/443`; the v57 release helper passed exact-hash `PreflightOnly`, created immutable v57 and one staging deployment, removed only the exact rolled-back v56 staging deployment, and restored HEAD to v55. Immutable v56 remains historical.
- Signed Telegram Desktop A/B reproduced the same generic mailbox error on staging v57 and two fresh production v55 launches. Production therefore remains v55 and the menu points to production.
- Apps Script Executions showed successful v57 `doPost`, `mailboxRedeemLaunch`, and `mailboxRpc`. The minute worker logged `Google OAuth token refresh request failed`, followed by `Service invoked too many times for one day: urlfetch` in `gmailApiRequest_`.
- This is a shared external quota blocker, not proof of a v57 regression. No promotion, OAuth repetition, Gmail mutation, trigger change, or GCP-project migration was performed.
- Resume only after quota recovery: establish two clean v55 launches, then execute the signed v57 account-root/switch A/B. A future code candidate, if evidence requires one, is immutable v58.
